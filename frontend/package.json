{
  "name": "solaris-frontend",
  "version": "3.0.0",
  "description": "SOLARIS AI Energy Intelligence — 3D Dashboard",
  "scripts": {
    "start": "npx serve . -p 3000",
    "build": "echo 'Static site — no build needed'",
    "dev": "npx live-server --port=3000"
  },
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.2",
    "serve": "^14.2.3"
  }
}
